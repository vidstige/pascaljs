program comparisions;
var
    x, y: Integer;
    z: Boolean;
begin
    x := 17;
    y := 99;
    WriteLn('x = y  ', x=y);
    WriteLn('x >= y ', x>=y);
    WriteLn('x <= y ', x<=y);
    WriteLn('x <> y ', x<>y);
    WriteLn('x > y  ', x>y);
    WriteLn('x < y  ', x<y);
end.